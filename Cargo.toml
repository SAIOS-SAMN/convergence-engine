# AI-GENERATED CONTENT — Generated by AI systems under human editorial direction.
# Not written by the researcher. See /opt/saios/DISCLAIMER.md
#
[package]
name = "saios-kernel-v2"
version = "2.0.0"
edition = "2021"
description = "SAIOS Kernel v2 — Genesis Engineering / Holonomic"
license = "UNLICENSED"
rust-version = "1.78"

[dependencies]
# Current kernel.rs dependency graph
# Core arithmetic — exact rational over Q
num-rational = { version = "0.4", features = ["serde"] }
num-bigint = { version = "0.4", features = ["serde"] }
num-traits = "0.2"
num-integer = "0.1"
# Serialization (socket responses only — not in hot data path)
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Error handling
thiserror = "1.0"
# Cryptography
tiny-keccak = { version = "2.0", features = ["keccak"] }
blake3 = "1.8"
ed25519-dalek = { version = "2.1", features = ["rand_core"] }
rand_core = { version = "0.6", features = ["getrandom"] }
# File locking
fs2 = "0.4"
tikv-jemallocator = "0.6.1"

[dev-dependencies]
proptest = "1.4"
tempfile = "3.27.0"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
float_arithmetic    = "deny"
cast_precision_loss = "deny"
