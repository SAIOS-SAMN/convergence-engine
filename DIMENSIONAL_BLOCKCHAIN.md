> **AI-GENERATED CONTENT** — This document was generated by AI systems under human editorial direction. It was not written by the researcher. See DISCLAIMER.md in this repository for full context.

# Dimensional Blockchain — A Consensus Network With Exact Measurement

## Date: April 16, 2026

---

## What This Is

A consensus network. Two or more nodes do independent work,
the system measures their agreement, and the result is
receipted. That's the foundation. Everything else builds on
it.

The difference from a traditional consensus network: the
agreement measurement is exact rational arithmetic, not
probabilistic voting. The difference from a traditional
blockchain: the receipt chain carries multi-dimensional
relational structure, not flat transaction records.

## How It Compares

### Traditional consensus network

Nodes propose. Nodes vote. Majority wins. The result is
binary: accepted or rejected. The losing minority's
perspective is discarded.

### Traditional blockchain

A flat chain. One block after another. Linear. One
dimension. Every block references the previous block.
Forward only. The chain verifies: did this transaction
happen? Yes or no.

### This system

Nodes work independently. The membrane measures their
agreement per cell across multiple relational dimensions.
The result is not binary — it is an exact rational number
measuring the distance from agreement in every relational
direction.

The chain verifies: did these independent nodes converge
to the same position in the same relational space? By how
much do they agree? In which dimensions do they disagree?
What is the exact rational distance between their
positions?

The consensus is not "did 51% agree." The consensus is
"did independent paths arrive at the same irreducible
form, and here is the exact measurement of the agreement
in every relational direction."

| Feature | Traditional blockchain | This system |
|---------|----------------------|-------------|
| Chain structure | Linear (one block references previous) | Two chains — temporal anchor + dimensional receipts |
| Agreement measurement | Binary (accepted/rejected) | Exact rational number per relational dimension |
| Disagreement handling | Minority discarded | Stasis — pauses, preserves disagreement for resolution |
| What it records | THAT something happened | WHERE in dimensional space it happened |
| Arithmetic | Integer/float | Exact rational (no rounding) |
| Verification | Hash of transaction data | Hash of relational position + temporal anchor |

## The Two-Chain Architecture

The system uses two chains that reference each other.
Neither works without the other.

### Chain 1 — The Temporal Anchor Chain

Linear. Immutable. Append-only. BLAKE3 hashed.
Parent-linked.

This chain carries time and nothing else:
- K-index (the system's internal time step)
- Wall-clock timestamp
- Hash of previous anchor

One dimension. One direction. Forward only. Every anchor
references the previous anchor. The chain cannot be
rewritten because each hash includes the previous hash.

This is the fixed reference point. Everything else in the
system — every measurement, every agreement, every
disagreement — is pinned to a specific temporal anchor.
The pin cannot be moved.

### Chain 2 — The Dimensional Receipt Chain

Multi-dimensional. Each receipt carries the entity's
position in relational space:
- RCF identity (which orbit class the entity is in)
- Coherence level (how close to irreducible form)
- Sluice state (locked, shifting, gated, corrupted)
- Entity's position in the relational manifold

Every dimensional receipt references a specific temporal
anchor from Chain 1. The measurement can be arbitrarily
complex. The pin to time is simple.

### How They Work Together

The temporal anchor chain says WHEN.
The dimensional receipt chain says WHERE in relational space.

Together they say: at this exact moment in time, these
independent entities were at these exact positions in
relational space, and they agreed by this exact rational
amount.

The temporal anchor cannot be faked — it is hashed and
parent-linked. The dimensional receipt cannot be moved —
it is pinned to the anchor. Convergence that did not happen
cannot be claimed — the receipts from independent nodes
either match or they don't, and the measurement is exact.

```
TEMPORAL ANCHOR CHAIN (one dimension — time)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  K=0         K=1         K=2         K=3
  [hash₀] ←── [hash₁] ←── [hash₂] ←── [hash₃]
     │            │            │            │
     ▼            ▼            ▼            ▼
DIMENSIONAL RECEIPT CHAIN (multi-dimensional — relational space)
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│ Node A  │  │ Node A  │  │ Node A  │  │ Node A  │
│ pos: x₁ │  │ pos: x₂ │  │ pos: x₃ │  │ pos: x₄ │
│ coh: .8 │  │ coh: .6 │  │ coh: .3 │  │ coh: 0  │
├─────────┤  ├─────────┤  ├─────────┤  ├─────────┤
│ Node B  │  │ Node B  │  │ Node B  │  │ Node B  │
│ pos: y₁ │  │ pos: y₂ │  │ pos: y₃ │  │ pos: y₄ │
│ coh: .9 │  │ coh: .5 │  │ coh: .2 │  │ coh: 0  │
└─────────┘  └─────────┘  └─────────┘  └─────────┘
                                            ▲
                                     Both reached
                                     coherence = 0
                                     at the same K.
                                     MeshAxiom
                                     crystallized.
```

## Why Dimensionality Matters

A flat blockchain can answer: did Node A and Node B agree
on the same value at the same time? Yes or no.

A dimensional blockchain can answer:
- Did they converge to the same irreducible form?
- In which relational dimensions do they agree?
- In which dimensions do they still disagree?
- What is the exact rational distance between them in
  each dimension?
- Is the disagreement structural (systematic gap) or
  incidental (different convergence speed)?
- What is the trajectory of their convergence — are they
  approaching agreement or diverging?

This information is preserved in the receipts. A reviewer
can audit not just WHETHER agreement happened, but HOW it
happened — through which dimensions, at what rate, with
what residual at each step.

## Why Exact Arithmetic Matters

Traditional blockchains use integer arithmetic for
financial transactions (counting tokens) or floating
point for computed values (smart contracts).

This system uses exact rational arithmetic
(`num_rational::BigRational`) throughout. The coherence
functional, the residual measurement, the entropy
calculation, the consensus ratio — all exact. No
rounding. No accumulated error.

This means the agreement measurement between nodes is
exact. When two nodes report coherence = 0 on the same
data, that is algebraically verified zero — not "close
enough to zero after floating point rounding." The
convergence is mathematical proof, not statistical
confidence.

## The Temporal Anchor as Fixed Reference

The temporal anchor chain provides the fixed reference
point that makes multi-dimensional measurement
meaningful. Without a fixed time reference, dimensional
measurements from different nodes cannot be compared —
you don't know if Node A's measurement at position X
happened at the same time as Node B's measurement at
position X.

The anchor pins every measurement to a specific moment.
The moment is immutable. The measurement is exact. The
comparison is therefore exact: at this specific moment,
did these nodes agree? By how much? In which dimensions?

The anchor is a zero-dimensional point (a timestamp)
that the entire multi-dimensional structure references
for verification. Verification happens at the anchor
point. Work happens in the dimensions. The anchor holds
the dimensions accountable to time.

## The Code

| Component | File | What it does |
|-----------|------|-------------|
| `TemporalLedger` | `chain/temporal_ledger.rs` | The two-chain structure — levels indexed by K-index |
| `MeshReceipt` | `chain/temporal_ledger.rs` | Dimensional receipt — RCF identity, coherence, sluice state, timestamp |
| `Level` | `chain/temporal_ledger.rs` | All receipts at one K-index — the temporal anchor point |
| `Level::finalized` | `chain/temporal_ledger.rs` | Consensus achieved when witness weight exceeds 0.5 |
| `Perspective` | `membrane.rs` | One node's dimensional position on shared data |
| `CompoundPerspective` | `membrane.rs` | Multi-node agreement measurement per cell |
| `MeshAxiom` | `membrane.rs` | Crystallized knowledge from independent convergence |
| `sigma_mesh` | `consensus.rs` | Weighted agreement aggregate across all peers |
| `coherence_functional` | `engine.rs` | Exact rational measurement of cocycle condition — the dimensional position |
| `coboundary_reduce` | `engine.rs` | H^1 projection to irreducible form — finding the convergence target |

## Summary

This is a consensus network with exact measurement and
dimensional structure.

- Two chains: temporal anchors (when) + dimensional
  receipts (where in relational space)
- Agreement measured in exact rational arithmetic across
  multiple relational dimensions
- Disagreement triggers stasis — the system pauses and
  preserves the disagreement for resolution
- Convergence is mathematical proof — exact zero, not
  statistical confidence
- The temporal anchor is immutable — every dimensional
  measurement is pinned to a specific moment
- The audit trail preserves not just WHETHER agreement
  happened, but HOW — through which dimensions, at what
  rate, with what residual

It is a consensus network. The additional features are
exact measurement, dimensional structure, and temporal
anchoring. These are engineering additions to a known
architecture, not a new paradigm.

---

*A consensus network with exact rational measurement.*
*Two chains: time (immutable) + dimensions (multi-dimensional).*
*Agreement is exact. Disagreement is preserved. The anchor holds.*
